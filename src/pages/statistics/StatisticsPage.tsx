import type { Application } from "../../types/index";

interface StatisticsPageProps {
  data: Application[];
}

const StatisticsPage = ({ data }: StatisticsPageProps) => {
  return <div>StatisticsPage</div>;
};

export default StatisticsPage;
